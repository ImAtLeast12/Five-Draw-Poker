from random import *
NUMCARDS = 52
DECK = 0
PLAYER = 1
COMP = 2

#cardLoc = [0] * NUMCARDS
suitName = ("hearts", "diamonds", "spades", "clubs")
rankName = ("Ace", "Two", "Three", "Four", "Five", "Six", "Seven", 
            "Eight", "Nine", "Ten", "Jack", "Queen", "King")
playerName = ("deck", "player", "computer")#This is where I chane who is playing
NUMPLAYERS = 2





def main():
    '''#Initilize Players
    player = Player()
    player.name = playerName(1)
    player.playerNum = 1

    computer = Player()
    computer.name = playerName(2)
    computer.isComputer = True
    computer.playerNum = 2'''

    deck = Deck()
    player = [] 

    for i in range(NUMPLAYERS):
        player.append(Player()) #makes an instance of the player class
        player[i].name = playerName[i+1] #seting their name
        player[i].playerNum = i+1 #Seting what number they are

    for i in range(5):
        for x in range(NUMPLAYERS):
            player[x].assignCard(deck)

            
    print("Location of all cards\n#" + "\t" + "card" + "\t" + "\t " + "Location")
    deck.showDeck()

    for i in range(NUMPLAYERS):
        print("\n" + player[i].name+ "'s Cards")
        player[i].showHand()
        
    '''
    GOALS
    clearDeck()
    shuffleCards()

    for (playerOn in range(1,numPlayers)):
        PrintName()
        ScoreHand()
        PrintScores()
        ScoreHand()
        ShowHand()

        if (PlayerOn is Computer):
            exchangeComputerHand()
        else:
            exchangePlayerHand()
        ScoreHand()

        PrintHand()
        EndPlayersTurn()

        ScorePlayer()
        PassToNextPlayer()

    printScores()
    if (numPlayers == 1):
        pass
    else:
        FindTheWinner
        Tell who wins'''


class Player():
    def __init__(self, name = "defalt", isComputer = False, hand = [], playerNum = 1):
        self.name = name
        self.isComputer = isComputer
        self.hand = []
        self.playerNum = playerNum

    def assignCard(self,deck): 
        legalCards = []
        for i in range(len(deck.cards)):
            if (deck.cards[i].assignedTo == DECK): 
                legalCards.append(i)
        randNum = int(random()*len(legalCards)) #chooses an index in legalCards
        deck.cards[legalCards[randNum]].assignedTo = self.playerNum
        self.hand.append(legalCards[randNum])#Adds the number to the players hand
        return deck

    def showHand(self):
        print(self.hand)

class Deck():
    def __init__(self, cards = []):
        self.cards = cards
        for i in range(NUMCARDS):
            self.cards.append(Card(i))

    def showDeck(self):
        x = []
        for i in range(NUMCARDS):
            print("{}\t{:5}    \t{}".format(i,self.cards[i].getCardName(),self.cards[i].assignedTo))

class Card():
    def __init__(self, index, assignedTo = 0):
        self.index = index
        self.assignedTo = assignedTo

    def getRank(self):
        return self.index % len(rankName)

    def getSuit(self):
        return self.index // len(rankName)

    def getRankName(self):
        return rankName[self.getRank()]

    def getSuitName(self):
        return suitName[self.getSuit()]

    def getCardName(self): #This is very similar to printing the cards out
        return str("{} of {}").format(self.getRankName(), self.getSuitName())

if (__name__ == "__main__"):
    main()

